import Header from '../Header/Header'
import Hero from '../Hero/Hero'
const Home = (props)=>{
    return (
        <>
            <Header/>
            <Hero/>
        </>
    )
}

export default Home;